Voici l'état complet du projet et comment vous pourriez vous répartir le travail :

---

## 🔴 Ce qui reste pour un prototype fonctionnel

### Fonctionnalités core
1. **Liaison Last.fm ↔ MP3** — le fichier qu'on vient de coder, à tester
2. **Preview auto pour toute la queue** — quand `nextTrack()` est appelé, fetch la preview Deezer si pas de `localUri`
3. **Playlists persistées en SQLite** — créer/supprimer/ajouter des tracks
4. **Profile stats réelles** — brancher `getStats()` sur le profile screen

### UI manquante
5. **Player full screen** — le slider ne fonctionne pas toujours bien, shuffle/repeat non fonctionnels
6. **Home dynamique** — "Continue Listening" avec les vraies dernières tracks jouées
7. **Library screen** — actuellement vide/mock, à brancher sur SQLite

### Technique
8. **Gestion erreurs Wi-Fi Sync** — si le téléchargement échoue à mi-chemin
9. **Métadonnées ID3** — lire artiste/titre/cover depuis les tags MP3 au lieu du nom de fichier
10. **Onboarding** — premier lancement, choisir son pseudo (AsyncStorage)

---

## 👥 Répartition suggérée

**Toi :**
- Preview auto queue (touche `useAudio` + `playerStore`)
- Player full screen (shuffle, repeat)
- Home dynamique
- Liaison Last.fm ↔ MP3 (à finir/tester)

**Coéquipier :**
- Playlists SQLite (il a déjà fait l'UI)
- Profile stats réelles (il a déjà fait l'UI)
- Library screen branché SQLite
- Onboarding / pseudo

**Ensemble :**
- Métadonnées ID3 (nécessite une lib native)
- Tests finaux + polish UI

---

Les tâches sont bien isolées — pas de risque de conflits si chacun touche ses propres fichiers. Tu veux qu'on attaque quoi en premier ?